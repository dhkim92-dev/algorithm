#include <string>
#include <vector>

using namespace std;

vector<int> solution(vector<long long> numbers) {
    vector<int> answer;

    for(auto num : numbers){
        bool check = false;

    }

    return answer;
}